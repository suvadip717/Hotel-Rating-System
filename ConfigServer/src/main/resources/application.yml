spring:
  application:
    name: ConfigServer

  cloud:
    config:
      server:
        git:
          uri: https://github.com/suvadip717/Microservice-Configuration
          clone-on-start: true

server:
  port: 8085
